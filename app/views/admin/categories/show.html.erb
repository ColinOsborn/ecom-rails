<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><% notice %></p>
    <% end %>

    <%= render @admin_category %>
  </div>
</div>
    <%= link_to "Edit Category", edit_admin_category_path(@admin_category), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to 'Remove Category', admin_category_path(@admin_category), method: :delete, class: "ml-2 rounded-lg py-3 px-5 bg-red-50 inline-block font-medium", data: { confirm: 'Are you sure?' } %>
    <%= link_to "Back to Categories", admin_categories_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>